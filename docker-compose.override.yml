services:
  app:
    environment:
      NODE_ENV: development
      NEXTAUTH_URL: http://localhost:3000
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
  db:
    ports:
      - '5432:5432'
